# Szimulátor handoff – 15. nap, fleet inventory és quorumtag-célzás

## 15. nap kiegészítés (2026-09-05 este) – a 16 hostos fleet-manifest elkészült, importra vár

A tényleges devnet fleet-deklaráció megvan: 16 host, 160 target (152 masternode + 8 fleet-staker), a séma szerint érvényes, és a read-only `POST /admin/simulations/targets/inventory-preview` 160 create / 0 update / 0 undeclared / 0 kockázatos mapping-változást ad rá. Ujjlenyomat: `41a4c2b02c448b9c5bcb2f7acbaa30f4f493c63b1c0a4df2d26dae7a9f65916c`, `inventoryId: devnet-fleet-2026-09-05`. **Importálva ugyanaznap este a felhasználó „csináld” jóváhagyásával:** 160 `PUT /admin/simulations/targets/:id` (ütemezve, mert a szimulátor admin-routere 30 kérés/perc), mind `enabled: false`; utána az előnézet 160 változatlant adott azonos ujjlenyomattal, a registry 160 targetet tart 16 hoston (152 masternode, 8 staker, 0 engedélyezett). Az engedélyezés (`enable`) továbbra is külön, safety-admin lépés. **Amit az import szabály szerint nem old meg:** a `GET /quorums/forming` feloldása most is `no unambiguous target mapping`-gel áll meg, egy `roland-node-8` tagnál. A resolver minden targetet elejt, amelynek hostjáról nincs `HostStatus` (`MISSING_HOST_OBSERVATION`), és observer csak a 8 DAO-hoston fut: azok 88 targetje feloldódik (friss, tipen, a build 64 hexes és egyezik az `expectedBuild`-del), a 8 megosztott `roland-node-*` host 72 targetje nem. Egy 60 tagú quorum 152-ből szinte mindig tartalmaz ezekből, ezért a feloldás addig zárva marad, amíg vagy observer nem fut a megosztott hostokon (döntés: termelő szolgáltatásokat visznek), vagy egy policy be nem engedi a deklarált, de nem megfigyelt targeteket – amit a fail-closed terv szándékosan nem tesz.

Hogyan készült, reprodukálhatóan és cím nélkül a repóban:

- `ops/fleet-inventory-collect.sh` a jump hoston fut: a 16 hostos inventory minden hostját csak olvasva kérdezi (root és nem-root login egyformán, a `fleet-dsl-enforcement.sh` transzportjával), és unitonként adja a portot, a külső címet, a szerepet (BLS-kulcs a confban → masternode, különben staker), a `masternode status` proTxHash-ét, az egység állapotát, hostonként pedig a `defcond` sha256-át és az observer címkéjét (`/etc/devnet-observer.env`). Eredmény: `/root/fleet-manifest-raw.txt` a jump hoston (0600).
- `ops/fleet-inventory-manifest.py` a VPS-en fut: a nyers listából és az explorer **privát** operátor-térképéből (`GET /admin/operators`) építi a manifestet, keresztellenőrzi az explorer láncból indexelt masternode-listájával (minden proTxHash létezik, és ugyanahhoz az operátorhoz tartozik, mint amelyik host confjában a kulcs van), `/root/fleet-manifest-devnet.json`-ba írja (0600), és lekéri az előnézetet. Cím sehol nem kerül kimenetre.

Döntések a mezőkről: `hostRef` a stabil címke – a 8 observeres hoston az observer címkéje (`fullnode-1,4-10`), mert a preflight frissesség-ellenőrzése a `HostStatus.host`-ra kulcsol, a másik 8 hoston az operátorcímke (`roland-node-1..8`); `chainHostRef` a privát cím, amit a resolver a lánc `service` mezőjével vet össze; `operatorId` az explorer operátorcímkéje; `unitRef` `defcon-devnet-mn@N`; `expectedBuild` a hoston mért 64 hexes sha256 (mind a 16-on `dde9b8b5…`, egyezik a propagation-végpont jelentésével); `capabilities` egyelőre csak `service-control` – `netem-p2p` majd a pilot host wrapper-újratelepítése után; `labels`: `devnet-fleet` + `dao` vagy `roland-shared` + `observer`. Target-azonosító: `<hostRef>-mnN`, a stakeré `<hostRef>-staker`. Limitek: 160 összesen, 14 hostonként (a legnagyobb host 14 masternode-ot visz).

Ellenőrzés a „verify the verifier” szabály szerint: az első futás 152 hibát jelzett, mert a nyilvános masternode-sor a hostot álnevesíti (`host-…`), és az összevetés a nem létező `hostIp` mezőt olvasta – az ellenőrzőt javítottuk operátorcímkére. Utána negatív kontroll: két host egy-egy proTxHash-ét felcserélve pontosan 2 hibát jelez és más ujjlenyomatot ad; a valódi manifest 0 hibával, `exit 0`-val zár. A staker-instance-ok (mn11 a 8 DAO-hoston) `role: staker`, proTxHash nélkül, ahogy a séma kéri.

Ami a 8 nem-observeres hoston ebből következik: a preflight ott „stale” hostot lát majd, mert nincs telemetria – az observer kiterjesztése a `roland-node-*` hostokra külön döntés (megosztott, termelő gépek).

## 18. nap (2026-09-05) – DSL fault-injection terv és Core tesztkeret: kész, PR-ban

Core PR: defcon-project/defcon#198 (`test/devnet-dsl-fault-injection`, az upstream `v22.1.x`-re; a `~/DEFCON-tests` worktree-ben készült). Tartalma: `dsl::CFaultInjector` (`src/evo/pose_service_faults.*`) – folyamat-lokális fault-állapot öt fajtával (`response-drop`, `report-drop`, `response-delay`, `report-delay`, `commitment-skip`), azonosítóval, kötelező `scenarioId`-vel és **magasság szerinti lejárattal** (aktív, amíg `height < expiryHeight`; a lejárati blokk már tiszta); halva született, névtelen és nulla késleltetésű faultot elutasít; semmi nem szerializálódik, nincs adatbázis és nincs fájl, restart = teljes helyreállás, konszenzusadatba/evodb-be/chainstate-be nem juthat. `-enablefaultinjection` hard gate: mainen és testneten indításkor elutasítva (a döntés a chainparams tiszta függvénye, mind a négy láncon egységtesztelve), devneten és regtesten elfogadva, log-figyelmeztetéssel; flag nélkül az injector kikapcsolva és inert. `faultinject set|list|clear` RPC csak a datadir-cookie-val hitelesített hívónak válaszol (rpcuser/rpcauth sosem), így az orchestrator a node-lokális wrapperen át éri el, mint minden más faultot. Terv: `doc/dsl-fault-injection.md` – mit csinál majd az egyes fajta és hol (announce, emit, signing, miner-inclusion), ez a 19–20. nap. Ellenőrzés: `dsl_fault_injection_tests` 6 eset zöld; `feature_dsl_fault_injection.py` regtesten zöld (felfegyverzett és sima node: set, lejárat bányászott blokkokkal, elutasítások, clear, restart mindent elfelejt, flag nélküli restart újra elutasít); a szomszédos DSL-suite-ok zöldek; a bináris konszenzus-string ujjlenyomata **bitre azonos** a seeden futóéval (178 string, azonos md5) – a `NodeContext` mezője ezért `faultinjector`, mert a `dsl_faults` név az `assert` szövegeként bekerült volna. Két tanulság a tesztkeretről: (1) a `test_defcon -t <suite>` egy fixtúra-abort után holtpontra fut a loggerben, `--run_test=` alakkal és esetenként futtatva derül ki a valódi hiba; (2) devnet-paraméter tesztben csak a `gArgs.SoftSetBoolArg("-devnet", true)` + `ForceRemoveArg` mintával építhető, lokális `ArgsManager`-rel a `GetDevNetName` assertál. Elfogadási kapu (mainneten sem configból, sem RPC-ből nem aktiválható): teljesül. Nem történt: fleet-, VPS- vagy laborbinárs-csere; a flag egyetlen devnet-node confjában sincs.

Aktuális nap/fázis: a 15. nap kódbeli biztonsági alapja elkészült a `feat/day15-fleet-quorum-snapshots` ágon. A tényleges fleet-deklaráció változatlanul nincs automatikusan felderítve vagy írva.

Kiegészítés (2026-09-05, `feat/day15-forming-quorum`) – a következő quorum feloldása a láncból. A korábbi „a következő quorum tagsága csak hiteles forrásból” nyitott kérdés lezárult: a hiteles forrás maga a lánc. A node v20 alatt a ciklus-kezdő blokk hash-éből és az akkori MN-listából választ tagokat, ezért a futó DKG quorumjának taglistája a kezdőblokk kibányászásától teljesen meghatározott. A `server/src/simulator/quorumMemberSelection.ts` ezt bitre pontosan reprodukálja (egyszeres SHA256 a `proTxHash‖confirmedHash` előhash-ből és a `SHA256d(típusbájt‖blokkhash)` módosítóból, csökkenő 256-bites sorrend, első `size`); a mellé tett fixture négy formált devnet quorumot tartalmaz (llmq_50_60 és llmq_defcon @8304, llmq_400_60 @8280, llmq_60_75 @8256, olvasva a seed node-ról 8320-as tipnél, service-címek nélkül), és mind a négyet **sorrenddel együtt** visszaadja, negatív kontrollal (más kezdőblokk vagy más típusbájt más listát ad). A `formingQuorum.ts` az eredményt csak azután ajánlja fel, hogy ugyanezzel a számítással előbb reprodukálta az adott profil utolsó formált quorumát; bukó önellenőrzésnél `next` üres, az ok kimondott, és a szerver hibaként naplózza. Minden más hiány is nevesített: commitolt ciklus és kibányászatlan következő kezdőblokk (a tagság addig nem létezik, nem csak ismeretlen), formálódási kapu alatti magasság, forgó vagy v20 utáni választás, regiszteren kívüli tag (ez a `current`-et nem blokkolja, a `next`-et a hiányzó tagszámmal együtt teszi nem elérhetővé). A referenciák `provenance` (`observed`/`computed`) és `verifiedAgainstQuorumHash` mezőt kaptak; az ujjlenyomat ezeket nem tartalmazza, a preflight snapshot-egyezése pedig csak a `current`-et hasonlítja, mert a formálódó quorum szabály szerint ciklusonként változik és még nincs kiválasztási bemenet, ami olvasná. Új RPC-burkoló: `protx diff 1 <h>` (az egyetlen, ami `confirmedHash`-t ad; a `protx list` állapota nem). Új privát végpont: `GET /quorums/forming?network=` – csak olvas; a 15. nap kapuja („ugyanazt a quorumot minden megfigyelő ugyanazzal a taglistával oldja fel”) ezen két observerről ellenőrizhető élesítés nélkül. Külső állapot: csak olvasó RPC a seed node-on a tesztvektorokért; VPS-módosítás, registry-írás nem történt. Nem készült el: a kiválasztás (`quorum-member-outage`, `phase: 'dkg'`) továbbra is a `current` quorumból válogat; a formálódó quorum célzása a 16. nap scenario-munkája, és külön döntést igényel, mert a DKG-ablak (~2 fázisblokk a kezdőblokk után) szűk. Ellenőrzés: `npm run typecheck` mindhárom workspace-en; teljes szerver-suite 160 fájl, 1449 teszt zöld (60 kihagyott, mint eddig).

Élesben is bizonyítva (2026-09-05, a #103 merge és deploy után). A VPS `.env`-je addig nem hordozta a szimulátor lánc-azonosító tűit (`SIMULATION_EXPECTED_CHAIN`, `SIMULATION_EXPECTED_GENESIS_HASH`), ezért ott minden szimulátoros bizonyíték-lekérdezés – a draft és a preflight is – `simulator chain identity pins are not configured` hibával 500-at adott; ezt eddig semmi nem kérdezte meg. A két értéket a felhasználó jóváhagyásával a node saját válaszaiból (`getblockchaininfo.chain`, `getblockhash 0`) írtuk be, a korábbi `.env` `.env.bak-*` néven megmaradt, a szolgáltatás újraindult. Ezután a `GET /quorums/forming` a 8335-ös tipnél a 8304-es formált `llmq_defcon` quorumot sorrendhelyesen reprodukálta (önellenőrzés: passed, orderMatched), és a **még nem commitolt** 8328-as ciklusra 60 tagot számolt. A commitment 8338 körül bányászódott ki; a `quorum info 7 <8328 hash>` 60 tagja **bitre és sorrendre azonos** az előre kiszámolt listával. Ez az első eset, hogy a formálódó quorum tagságát a commitment előtt neveztük meg, és a lánc utólag igazolta.

Ugyanez a lekérdezés két nyitott hiányt is megmutatott, a szimulátoron kívül. (1) A VPS target-regisztere üres: nulla devnet target, ezért a `resolution` a `current` quorum egy tagjánál `no unambiguous target mapping`-gel megáll – ez a 15. nap ismert, jóváhagyásra váró manifest-tétele, nem hiba. (2) Telemetria csak 9 hostról érkezik (`fullnode-1,4-10` és `seed`), a `nodeBuild` 12 hexes, a seedé üres; a teljes 16-hostos regisztráció friss telemetriát és 64 hexes buildhash-t kér minden hostról, tehát a manifest a 13–14. napi observer/pilot-rollout mögött áll. Mellékesen: `fullnode-9` más buildet jelent (`a220f5…`) mint a másik hét (`baa33a…`). Tisztázva a jump hostról, csak olvasva: mind a 16 hoston **ugyanaz** a bináris (`sha256 dde9b8b5…`, md5 `c0db9a2f`, 2026-09-05 délután cserélve), és **egyik jelentett hash sem ez**. Az observer a buildhash-t a folyamat élete alatt egyszer számolta; hét observer 08-26 óta, a fullnode-9-é 09-01 óta fut, mindegyik az indulásakor látott binárist jelenti. A telemetria build-mezője tehát tíz napja minden rolloutot elnyelt, és a fullnode-9 „eltérése” nem drift, hanem egy későbbi observer-indítás. Javítva (#105): a cache a fájl inode/méret/mtime azonosítójára kulcsolt, nem a folyamatra (`ops/devnet-observer.py`). A javított szkript a felhasználó jóváhagyásával 2026-09-05 este a jump hoston át a 8 observert futtató hostra került (`install` a meglévő helyre, env-fájl érintetlen, `systemctl restart devnet-observer`), minden egység `active`, a szkript sha256-a mind a nyolcon a stagelt `59f5d236…`; a nyilvános propagation-végpont ezután mind a 8 hostról a lemezen lévő `dde9b8b5…` binárist jelentette, 8345-ös magasságnál. A másik 8 hoston nincs observer, azok érintetlenek. A `seed` sor nem observerből jön: az explorer saját `seedStatus.service.ts`-e írja RPC-ből, buildhash nélkül, és a VPS-en nincs is `devnet-observer` egység – a korábbi „rossz daemon-útvonal” magyarázat téves volt. Ha a seed buildje is kell a manifesthez, a seedStatus szolgáltatásnak kell a `/usr/local/bin/defcond`-ot hash-elnie; nyitva.

Elkészült: a target-regiszter fail-closed kaput kapott engedélyezett összes és hostonkénti célpontszámra (alapérték: 20, illetve 10; policyból szigorítható). Az aktuális és csak hitelesen megfigyelt következő quorum egy `llmqType/name + quorumHash + expectedHeight + quorumIndex + member proTxHash` azonosítóhoz kötött, kanonikus snapshot. A quorumtagok input-sorrendjétől és observer tipmagasságától függetlenül ugyanarra a `proTxHash -> targetId` feloldásra jut; hiányzó vagy kétértelmű mappinget elutasít. Következő quorum-taglista nélkül a rendszer explicit „nem elérhető” állapotot rögzít, nem jósol tagokat. Armkor append-only `armed-target-snapshot` artifact készül, és start csak ennek sértetlen, a planhez kötött változatával folytatódhat. Quorum-outage preflight a rögzített azonosított quorum eltérését is blokkolja.

Módosított fájlok: `server/src/simulator/quorumTargetSnapshot.*`, `targetResolver*`, `draftPreparation*`, `preflight*`; `server/src/services/simulationEvidence.service.ts`, `simulationControl.service.ts`; `server/src/models/SimulationRun.ts`, `SimulationRunArtifact.ts`; érintett model/audit/persistence/control tesztek.

Futtatott tesztek és eredményük: `npm run typecheck` zöld; `npm test` zöld (68 fájl, 618 teszt); `npm run build` zöld a `server/` workspace-ben.

Nyitott kérdés: az ütemterv 8 hostot említ, a korábbi read-only operátori felmérés 11 MN hostot talált. Emiatt a kód nem hardcode-olja a 8-at. Az explicit, jóváhagyott inventory-manifesztet kell betölteni/ellenőrizni, majd a policy-limitet ehhez rögzíteni. A következő quorum tagsága csak akkor oldható fel, ha azt egy hiteles forrás teljes taglistával közli.

Külső állapot/VPS-művelet: nem történt. Aktív fault vagy recovery timer: nincs. Felhasználói jóváhagyás: a manifest tényleges fleet-regiszterbe írásához és bármely VPS-művelethez szükséges.

Kiegészítés – Day 15 manifest-előkészítés: a `fleetInventoryManifest` csak explicit, schema-versionált deklarációt fogad el. Kötelező benne a hálózat, a várt hostszám, az összes/hostonkénti limit, valamint minden target unit/port/proTxHash mappingje. Duplikált target, host/unit, host/port vagy proTxHash; hiányzó MN-proTxHash; hostszám- vagy limiteltérés fail-closed. A privát admin `POST /targets/inventory-preview` ugyanennek kizárólag olvasó diffjét adja: create/update/unchanged, a manifestből hiányzó régi registry-bejegyzések, és az engedélyezett targetek kockázatos mapping-változásai. Nem ír, nem töröl és nem engedélyez targetet. A teljes fleet 16 hoston 152 aktív MN; a korábbi 11 hostos, rootos rollout-lista csak részhalmaz (115 instance, ebből 107 proTx-státusszal), ezért abból nem készül manifest. A teljes inventory limitje 250, hostonként 50; ez külön van a minden runra változatlanul érvényes 20-as selection limittől. A teljes leltár csak a 16 hostos, jogosultsághelyes ellenőrző forrásból állítható elő. A teljes 16-hostos regisztrációhoz minden hostról friss telemetria és teljes, 64 hexes buildhash kell. Az observer installer már nem egy mindenhol feltételezett `mn11` unitra vár: ha az előző rolloutból van `mn11`, azt használja, egyébként `mn1`-et; staker-scriptek méréséhez a datadir explicit megadható. A publikus observer címkéje kötelezően stabil, nem címérték; ezt a manifest `hostRef`, a lánc felőli privát címet a `chainHostRef` hordozza. Külső telepítés vagy registry-írás nem történt. A megemelt plafon továbbra is plafon: saját teszt őrzi, negatív kontrollal igazolva (a plafont megemelve a teszt elbukik). Mért méret: egy 152 targetes devnet manifest ~68,6 kB, 250 target ~113 kB — a produkciós szerver 256 kB-os `express.json` limitje alá fér, a `labServer` 64 kB-osa alá nem, így egy nagy regtest manifest ott 413-mal (fail-closed) állna meg, nem rossz válasszal. Ellenőrzés: `npm run typecheck`; teljes szerver-suite (69 fájl, 626 teszt) és build zöldek.

---

Aktuális kiegészítés (2026-09-04): a 11. napi adminpanel-vezérlés helyi implementációja elkészült. A `/admin` oldalon a kezelő csak a szerver által allowlistelt scenario-kból készíthet plan-t; minden paramétert a szerver Zod-sémája validál. A felület megmutatja az exact action-listát, célokat, DKG/ChainLock margót és figyelmeztetéseket, majd külön preflightot kér. A szerver-deklarált kockázati osztályt checkboxszal kell elismerni (high csak safety-adminnak), az `armed` futam indításához pedig egy második, külön checkboxos megerősítés kell. Minden módosító kérés session-CSRF + újrapróbálható idempotency key-t hordoz. Aktív live runnál másodpercenkénti lease-visszaszámláló, azonnali `Abort & recover`, és a visszaállítási bizonyíték látható; a szerver változatlanul csak helyi lab `regtest` live executorát engedi. Ellenőrzés: teljes `npm run typecheck`; teljes szerver suite (67 tesztfájl, 607 teszt); `npm run build -w client` zöld. Külső állapot/VPS-művelet nem történt; Docker daemon hiányában valódi browser–proxy–labor E2E továbbra sem futott. Hátra: az Access-konfiguráció és jóváhagyott deploy/E2E, majd a 11. nap laboros kontrollkapuja (Gate B) tényleges bizonyítása.

Aktuális kiegészítés (2026-09-04, ez a bejegyzés felülírja az alábbi korábbi „HÁTRA” sort): a 10. nap `/admin` kliens-alapja elkészült. A saját `dd-admin-shell` csak az azonos originen lévő session-ajton keresztül lép be; sem API-kulcsot, sem infrastruktúra-credentialt nem ismer. A cookie HttpOnly marad, a CSRF-token kizárólag a lap memóriájában van. A felület elkülönül a publikus navigációtól, és ma csak olvasható: health/build/target-regiszter, allowlistelt scenariók száma, aktív live-slot, a redaktált futamlista és a választott futam privát, append-only action timeline-ja látszik. Scenario- vagy fault-vezérlő nem került bele; az a 11. napi kétlépcsős megerősítés része. Elkészült a Cloudflare Access JWT-kapu is: `jose` a team-domain forgó JWKS-ét használja, RS256 aláírást, exact iss-t és az alkalmazás AUD-tagját ellenőrzi, majd kizárólag a verifikált `email` claimet vezeti át a szerveroldali allowlistba. `NODE_ENV=production` alatt e két Access-konfiguráció nélkül a böngészős belépés fail-closed; a lab fejlesztői, szűk proxy + identitásfejléces útja megmaradt. Az új `ADMIN_CLOUDFLARE_ACCESS_TEAM_DOMAIN` és `ADMIN_CLOUDFLARE_ACCESS_AUDIENCE` értékeket a Zero Trust Access alkalmazásból kell majd kitölteni. Ellenőrzés: teljes `npm run typecheck`; `npm run build -w server`; `npm run build -w client`; teljes szerver suite (67 tesztfájl, 607 teszt) zöld. Lokális Docker daemon jelenleg nem elérhető, ezért böngésző–proxy–labor end-to-end próba nem történt. Külső állapot/VPS-művelet nem történt. Következő külső lépés: a két Access-érték biztonságos beállítása, majd jóváhagyott VPS-deploy és valódi Access JWT-vel végzett belépési próba. `npm audit --omit=dev` jelenleg három, a meglévő Express → body-parser → qs láncban jelzett mérsékelt hibát mutat; az audit-fix dry-run sem talált alkalmazható frissítést.

Aktuális nap/fázis: 9. nap kesz, Gate A tartalmilag kimondhato (mind a negy scenario abort-teszten atment, ismetelhetoseg kontrollal). A 10. NAP SZERVEROLDALA KESZ es elesben bizonyitott (11 ellenorzes): bongeszos bejelentkezes proxy-altal-allitott identitassal, csak listazott proxytol es csak listazott alanynak; szerveroldali session (a cookie 256-bites azonositot hordoz, a sor annak sha256-jat), HttpOnly + SameSite=Strict + admin-utvonal + Secure; CSRF-token minden modositason; kulcs + session egy keresen = 400; a kulcs-ag VALTOZATLANUL utasitja el a bongeszot, es negativ kontroll bizonyitja, hogy a teszt ezt fogja. A labor most a produkcios utvonalakat mountolja (/api/v1/admin/...), mert a cookie hatokore a labor-sajat alakot sosem erte volna el. HATRA a 10. napbol: a frontend (/admin felulet) -- koordinacios kerdes a client/-en dolgozo masik AI-jal; es a Cloudflare Access JWT tenyleges ellenorzese a fejlec-bizalom helyett produkcion
Branch és HEAD: `chore/lab-scale-10`; mergelve: PR #50 (kód-egység rendezés), #53 (mérési hurok + audit-visszajátszás javítás), #54 (labor-telemetria), #56 (action-diszpécser), #57 (partition), #58 (LLMQ-profil deklaráció)
Elkészült a 8. napból: node-local netem wrapper daemon egyetlen tulajdonossal és saját TTL-watchdoggal; második fault-osztály (konténer stop/start), (konténer, osztály) párra kulcsolt állapottal és a jobból származtatott undóval; valós `DockerLiveExecutor` a control-API három slotjában, fail-closed minden fault-fajtára, amit nem tud hitelesen alkalmazni; kompozit netem (latency+jitter+loss egy qdisc-ben); coincidence-preset az `IsMiningPhase` pontos reprodukciójaként, mind a hat profilra
Elkészült az auditból: (1) az observer containerenként a SAJÁT node-ját kérdezi, és amit nem ér el, arról NEM jelent -- a compose ehhez node-onként publikál RPC-portot, mert az image-ben nincs `defcon-cli`; (2) a regiszter kétlépcsős modellje kikényszerítve (a deklaráció nem tud engedélyezni; az engedélyezés safety-admin), plusz Compose-projekt allowlist az executoron, az első enqueue ELŐTT; (3) a cooldown határideje az állapotra kerül belépéskor (opcionális mező, `canonicalJson` ejti az `undefined`-ot, ezért a korábbi ujjlenyomatok bitre azonosak); (4a) a parancs claim-elt, nem fogyasztott: pending -> inflight -> gone, at-least-once, mert minden wrapper-parancs idempotens, és a kísérlet a CLAIM pillanatában íródik ki, hogy egy folyamatot megölő parancs is karanténba kerüljön; (4b) a lease abszolút pillanat, a már lejártat elutasítja (a `minLeaseMs` padló megszűnt); (4c) a live lock atomi CAS-szal, a futam saját borítékát használva lease-nek, mindkét recovery-ágon elengedve; (4d) egy megismételt start/recover a rekordból válaszol, mert minden esemény-azonosító a request-kulcsból származik
Elkészült a laborhoz: külön `labServer.ts` entrypoint saját, KÖTELEZŐEN külön adatbázissal; célpont-regiszter írója az admin API-n; wrapper-heartbeat; blokk-indexer, masternode-poller, list-diff és quorum-kollektor a laborban; `ops/lab-observer.mjs`. Erre épül a masternode bring-up (`ops/lab-bringup.mjs`) és a futtatókörnyezet: `lab-serve`, `lab-wrapper`, `lab-observe`, `lab-miner`, `lab-register-targets`, `lab-mine-quorum`, `lab-walkthrough`, közös `lab-rpc` és `lab-paths`. Négy dolognak kellett igaznak lennie, hogy egyáltalán kvórum formálódjon, és mind a négy NÉMÁN bukott: a konténerek IP-jét rögzíteni kell (a ProTx a szolgáltatáscímre van kötve, a Docker indítási sorrendben oszt címet, így az operátorkulcsok kiosztása utáni recreate permutálta őket); a sporkokat be kell kapcsolni (mind 4070908800, azaz KI -- SPORK_17 nélkül egyetlen DKG-session sem fut, a lánc mégis bányász commitmenteket, csak null-okat); a node-oknak el kell érniük az IsSynced-et (egy masternode alatta nem fogad bejövő kapcsolatot, így minden kvórum-kapcsolat `connected: false`); és lassan kell bányászni (a hat DKG-fázis magasságvezérelt, egy intervallum egyben kibányászva ezredmásodpercek alatt átfut)
Módosított fájlok: `simulator/netemLease|netemRunner|netemWrapperHost|netemWrapperMain|labCompose|liveExecutorPlan|dockerLiveExecutor|wrapperHeartbeat|targetRegistration|labAcceptance|dkgCoincidence`; `domain/simulationRunState|labIsolation|liveRunLock`; `services/simulationControl|simulationEvidence|simulationLiveRunLock`; `routes/v1/simulationAdmin.v1`; `labServer.ts`; `config.ts`; `ops/lab-observer.mjs`; `.env.example`; minden érintett tesztfájl
Futtatott tesztek és eredményük: `npm run typecheck` és a teljes szerver-suite (114 fájl, 994 teszt) 0-val tér vissza. FIGYELEM: a kapukat exit-kóddal kell ellenőrizni -- a `| tail -1` elnyeli, és egyszer emiatt commitoltam bukó typecheck mellett. A valódi bizonyíték azonban a végigmérés: a három hibát, amit talált, EGYIK unit teszt sem láthatta, mert a repository in-memory
Nyitott hibák: nincs. A nap tizedik hibaja is javitva (8762166): az observation sweep egy tul rovid fault-ablaku futamot (aktivalas utan 8 s-cel abortalva, tehat a warm-up kizaras utan ures ablak) orokke ujraprobalt es 20 masodpercenkent hibakent naplozta. A meresi hatar most kodot ad a visszautasitasnak (WINDOW_UNMEASURABLE); a sweep egyszer feljegyzi a futamon (gyokerszintu measurement.unavailable mezo, szandekosan nem a state al-semaban, mert az az audit stateAfter-je is), info-kent naplozza, es a jelolt-lekerdezes tobbe nem kinalja fel. Bizonyitek a laboron: ujrainditas utan a beragadt futam egyetlen info-sort kapott, a Mongo-ban measurement.unavailable=true, es 282 s (kb. 14 sopres) alatt nulla ismetelt hibasor a korabbi 27 helyett. Ugyanebbol az osztalybol a tizenegyedik: a kor-felvevo quorum info hivasa a commitment banyaszasanak pillanataban "quorum not found"-ot kap (a lista mar latja, a kvorum-cache meg nem), amit a hivo helyesen atmenetinek vesz es ujraprobal -- de az RPC-reteg elobb hibakent naplozta, minden kvorumalakulasnal harom sort. Az RPC-hivas most kap egy tolerated opciot, amivel a hivo megnevezi a visszautasitast, amit maga kezel, es az info-szintre kerul; minden mas visszautasitas tovabbra is hiba. Es a tizenkettedik, amit az elozo kettonek a nyoma vezetett el: a szinkron a commitment-sorok punishedCount mezojet 96%-ban null-kent irta (3632 a 3771-bol). Ket ok: a tartomany-fuggveny elobb a taglista hianyat nezte, mint a null-commitment szabalyt (egy hibas DKG definicio szerint senkit nem buntet, de a kvorumot a csomopont sosem tudja leirni, mert nem is epult -- 3145 sor), es a csucsnal a szinkron 15 ms-mal a blokk utan kerdezett, amikor a csomopont kvorum-cache-e meg nem epult fel (merve: a kvorum kb. 200 ms-mal a blokk utan listazodik, es kb. 200 ms-mal kesobb irhato le), a sikertelen valaszt pedig a folyamat elettartamara gyorsitotarazta. Javitas: a null-commitment ellenorzes megelozi a taglista-ellenorzest; a feloldo csak sikert gyorsitotaraz, es a csucsnal 3x300 ms-ot var; a mar beirt null-okra a fizetett-visszatoltes mintajara egy hatarolt visszatolto sopres fut (100/menet, legujabbtol, azonos duplazodo utemezes). Bizonyitek a laboron: 37 menet, 55 s, 3787-bol 0 ismeretlen, 0 ujraprobalas, 0 hibasor. A nap kilenc korabbi hibaja mind javitva (lasd a korabbi verziot es a commit-uzeneteket)
Külső állapot: VPS-műveletet nem végeztünk. Lokálisan fut a labor: `lab-mongo` (27018) és a generált compose `mn01`-`mn04` (RPC 19800-19803, fix IP 172.28.0.2-.5), plusz a régi `lab-node` (19998), ami már nincs használatban. A generált `lab-compose.yml` és a `lab-masternode-keys.json` BLS operátorkulcsokat tartalmaz, ezért gitignore-ban vannak, ahogy a `.lab-state/` is (admin-kulcs, ingest-token, wrapper-állapot). A `~/DEFCON` checkoutban egy másik session dolgozik, és a `client/` mappán egy másik AI dolgozik -- oda nem nyúltunk
Aktív fault vagy recovery timer: nincs. A laborhoz OT folyamat kell: lab-serve, lab-wrapper, lab-miner (15 s/blokk), lab-observe --nodes 11, lab-keeper --nodes 11. A lab-serve most a bongeszos ajtot is felkonfiguralja: x-lab-identity fejlec, megbizott proxy 127.0.0.1/::1, alanyok lab-admin@local (safety-admin) es lab-operator@local (operator), Secure=false -- ez az egyetlen hely, ahol az lehet, es ki van mondva
Felhasználói jóváhagyás szükséges-e: a lokális Docker-munkához nem; bármilyen VPS-műveletnek, a 13. napi pilotnak és minden devnet-fault futtatásnak igen
Egyéb, amit tudni kell: a node EGYETLEN RPC-n sem adja meg az LLMQ-paramétereket (végignézve: list, listextended, info, dkginfo, dkgstatus, memberof, sign, verify) -- ezért `LLMQ_PROFILE_OVERRIDES`, és ezért kell a labornak a saját compose-ából származtatnia a deklarációt. A `-llmqtestparams=10:6` valójában 10/6/6/6: a node a minSize-t, threshold-ot ÉS dkgBadVotesThreshold-ot is a threshold-ból állítja. A lab image-ben NINCS iptables és nincs nft -- a partíció ezért tc-vel épül (prio gyökér + 100% loss sáv + peerenkénti u32 szűrő), és `docker exec -u root` kell hozzá, mert a konténer nem rootként fut. Egy fault-ciklus ~20 perc a laborban (ban, 10 perces ISLock-várakozás a revive-ra, ~2 DKG-kör a rendeződésre), és minden mempoolba kerülő tranzakció 10 percet vár -- ez az ütemezés valódi költsége, nem a kódolás
