Aktuális nap/fázis: 8. nap LEZÁRVA; a labor végigvisz egy élő futamot (create -> ... -> fault_active -> recover -> cooldown), és MÉRT IS: a 43 másodperces mn02-leállítás kihagyatott vele legalább két DKG-hozzájárulási ablakot, amiért 100 pontos PoSe-büntetést és bánt kapott a 995-ös magasságon -- pontosan az a jelenség, amit a CLAUDE.md leír. A munkamegbízás 4-7. tétele kész. A 9. nap (action-diszpécser, Gate A) NEM kezdődött el
Branch és HEAD: `feat/dkg-window-anchoring` a `feat/lab-masternodes` tetején (PR #48 nyitva); minden korábbi a mainben, PR #35, #37, #41, #42, #44, #45, #47
Elkészült a 8. napból: node-local netem wrapper daemon egyetlen tulajdonossal és saját TTL-watchdoggal; második fault-osztály (konténer stop/start), (konténer, osztály) párra kulcsolt állapottal és a jobból származtatott undóval; valós `DockerLiveExecutor` a control-API három slotjában, fail-closed minden fault-fajtára, amit nem tud hitelesen alkalmazni; kompozit netem (latency+jitter+loss egy qdisc-ben); coincidence-preset az `IsMiningPhase` pontos reprodukciójaként, mind a hat profilra
Elkészült az auditból: (1) az observer containerenként a SAJÁT node-ját kérdezi, és amit nem ér el, arról NEM jelent -- a compose ehhez node-onként publikál RPC-portot, mert az image-ben nincs `defcon-cli`; (2) a regiszter kétlépcsős modellje kikényszerítve (a deklaráció nem tud engedélyezni; az engedélyezés safety-admin), plusz Compose-projekt allowlist az executoron, az első enqueue ELŐTT; (3) a cooldown határideje az állapotra kerül belépéskor (opcionális mező, `canonicalJson` ejti az `undefined`-ot, ezért a korábbi ujjlenyomatok bitre azonosak); (4a) a parancs claim-elt, nem fogyasztott: pending -> inflight -> gone, at-least-once, mert minden wrapper-parancs idempotens, és a kísérlet a CLAIM pillanatában íródik ki, hogy egy folyamatot megölő parancs is karanténba kerüljön; (4b) a lease abszolút pillanat, a már lejártat elutasítja (a `minLeaseMs` padló megszűnt); (4c) a live lock atomi CAS-szal, a futam saját borítékát használva lease-nek, mindkét recovery-ágon elengedve; (4d) egy megismételt start/recover a rekordból válaszol, mert minden esemény-azonosító a request-kulcsból származik
Elkészült a laborhoz: külön `labServer.ts` entrypoint saját, KÖTELEZŐEN külön adatbázissal; célpont-regiszter írója az admin API-n; wrapper-heartbeat; blokk-indexer, masternode-poller, list-diff és quorum-kollektor a laborban; `ops/lab-observer.mjs`. Erre épül a masternode bring-up (`ops/lab-bringup.mjs`) és a futtatókörnyezet: `lab-serve`, `lab-wrapper`, `lab-observe`, `lab-miner`, `lab-register-targets`, `lab-mine-quorum`, `lab-walkthrough`, közös `lab-rpc` és `lab-paths`. Négy dolognak kellett igaznak lennie, hogy egyáltalán kvórum formálódjon, és mind a négy NÉMÁN bukott: a konténerek IP-jét rögzíteni kell (a ProTx a szolgáltatáscímre van kötve, a Docker indítási sorrendben oszt címet, így az operátorkulcsok kiosztása utáni recreate permutálta őket); a sporkokat be kell kapcsolni (mind 4070908800, azaz KI -- SPORK_17 nélkül egyetlen DKG-session sem fut, a lánc mégis bányász commitmenteket, csak null-okat); a node-oknak el kell érniük az IsSynced-et (egy masternode alatta nem fogad bejövő kapcsolatot, így minden kvórum-kapcsolat `connected: false`); és lassan kell bányászni (a hat DKG-fázis magasságvezérelt, egy intervallum egyben kibányászva ezredmásodpercek alatt átfut)
Módosított fájlok: `simulator/netemLease|netemRunner|netemWrapperHost|netemWrapperMain|labCompose|liveExecutorPlan|dockerLiveExecutor|wrapperHeartbeat|targetRegistration|labAcceptance|dkgCoincidence`; `domain/simulationRunState|labIsolation|liveRunLock`; `services/simulationControl|simulationEvidence|simulationLiveRunLock`; `routes/v1/simulationAdmin.v1`; `labServer.ts`; `config.ts`; `ops/lab-observer.mjs`; `.env.example`; minden érintett tesztfájl
Futtatott tesztek és eredményük: `npm run typecheck` és a teljes szerver-suite (114 fájl, 994 teszt) 0-val tér vissza. FIGYELEM: a kapukat exit-kóddal kell ellenőrizni -- a `| tail -1` elnyeli, és egyszer emiatt commitoltam bukó typecheck mellett. A valódi bizonyíték azonban a végigmérés: a három hibát, amit talált, EGYIK unit teszt sem láthatta, mert a repository in-memory
Nyitott hibák: ismert kódhiba nincs. A végigmérés HÁROM valódi hibát talált, amit a 990 unit teszt nem láthatott (mind javítva, mind regressziós teszttel): (1) a `cooldownExpiresAtMs` az állapoton volt, de a Mongoose al-sémán nem, ami `strict: 'throw'` és egyben az audit-esemény `stateAfter`-e -- így MINDEN élő futam elhalt az első preflightnál, és a tesztek in-memory repositoryjának nincs sémája; (2) a `hostRef` két dolgot jelentett (a konténer, amit az executor a `docker`-nek ad, és a host, amit a lánc lát) -- a devneten ugyanaz a sztring, a laborban nem lehet az, ezért egy labor-masternode feloldhatatlan volt, helyes érték nélkül: új, opcionális `chainHostRef`, a hiánya azt jelenti, hogy megegyezik, tehát a devnet viselkedése és ujjlenyomatai bitre változatlanok; (3) a leltárhiba kódokat sorolt fel, célpontokat sosem, így egy nyugdíjazott bejegyzés úgy olvasódott, mint a kért célpontok hibája. Tudatosan halasztva: a heartbeat boot-kori qdisc-próbája. Roadmap szerint hiányzik: action-diszpécser, automatikus `begin_observation`, automatikus measurement finalize, restart-flapping, staggered reconnect, partition
Külső állapot: VPS-műveletet nem végeztünk. Lokálisan fut a labor: `lab-mongo` (27018) és a generált compose `mn01`-`mn04` (RPC 19800-19803, fix IP 172.28.0.2-.5), plusz a régi `lab-node` (19998), ami már nincs használatban. A generált `lab-compose.yml` és a `lab-masternode-keys.json` BLS operátorkulcsokat tartalmaz, ezért gitignore-ban vannak, ahogy a `.lab-state/` is (admin-kulcs, ingest-token, wrapper-állapot). A `~/DEFCON` checkoutban egy másik session dolgozik, és a `client/` mappán egy másik AI dolgozik -- oda nem nyúltunk
Aktív fault vagy recovery timer: nincs. A laborban fut a bányász (15 s/blokk), az observer (4 s), a wrapper és a lab-szerver, egymáshoz hangolt kadenciákkal. FONTOS: fault-futam után a célpont banolva maradhat, és egy banolt masternode nem kvórumtag, ezért a `quorum-stable` kevesebb tagot old fel, mint ahány célpont van -- `ops/lab-revive.mjs` küld ProUpServTx-et, de az tíz percet vár a mempoolban, mert InstantSend-kvórum híján a `WAIT_FOR_ISLOCK_TIMEOUT` mindig teljes egészében lejár
Felhasználói jóváhagyás szükséges-e: a lokális Docker-munkához nem; bármilyen VPS-műveletnek, a 13. napi pilotnak és minden devnet-fault futtatásnak igen
Egyéb, amit tudni kell: a lab image-ben NINCS `defcon-cli`, csak `defcond` -- a node-ot csak HTTP RPC-n lehet kérdezni, konténerenként a saját publikált portján, a saját cookie-jával. A `bls generate` ezen a buildnél pénztárcához kötött, ezért az operátorkulcsok a pénztárcás node-on készülnek. A `register_fund` a COLLATERAL-címről finanszíroz, ha nem adsz `fundAddress`-t -- az frissen készült, tehát mindig üres. Az operátor-titkok nem nyerhetők vissza a láncról (a ProTx csak a publikus kulcsot rögzíti), ezért a bring-up kiírja őket, különben egy második futás minden masternode-ot néma módon sima node-dá fokozna le. A regtest spork-kulcs az upstream publikált teszt-kulcsa, csak a regtest spork-címre érvényes. A célpont `expectedBuild`-je 64 hex, tehát sha256, nem a flottánál szokásos md5. A `masternode/payments.cpp:55` összeomlás (null egy `gsl::not_null`-ban) továbbra is Core-oldali, determinisztikus kiváltó nélkül
