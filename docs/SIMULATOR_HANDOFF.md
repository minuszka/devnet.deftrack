Aktuális nap/fázis: 8. nap – LEZÁRVA. Block 1 (executor-váz, lease, recovery), block 2 (peerező compose-generátor, Dockerfile, node-local netem wrapper daemon, live executor) és block 3 (service fault-osztály: konténer stop/start) kész, tesztelt, és a nap elfogadási kapuja valós Dockeren bizonyítva. A 9. nap (Docker/regtest scenario-k, Gate A) következik
Branch és HEAD: `feat/simulator-day8-service-fault` – block 1/2 a mainben (PR #16–#21, HEAD `35b306e`); block 3 ezen a branchen
Elkészült (block 3 – service fault-osztály): a wrapper állapotgépe két fault-osztályt visel (`netem` | `service`), és az állapot (konténer, osztály) párra kulcsolódik, nem konténerre – egy service-job így nem tud kiejteni egy élő qdisc-rekordot (ez a superset-invariáns egyetlen osztályok közti törése volt); a job maga nevezi meg a visszavonását (`undoFor`), így minden undo-hely – explicit clear, TTL-sweep, boot-recovery – osztályfüggetlen; a service undo mindig MEGELŐZI a netem undót, mert a `docker start` új netns-t húz és a qdisc-et magával viszi, tc pedig leállt konténerben nem fut; `planServiceStop` pontosan `planApply` mintája (idempotens, referencia szerint ugyanaz az állapot); `stopService` a record-then-act sorrenddel (egy rekord nélkül landolt stop olyan node, amit soha semmi nem indít újra); a sweep best-effort és csak azt ejti ki, aminek a visszavonása tényleg megtörtént (egy elérhetetlen konténer nem tarthat lent egy egész restart-stormot); a boot-recovery SOHA nem dob, és amit nem tudott visszavonni, azt `expiresAtMs: 0`-val megtartja, hogy a következő tick újrapróbálja; a watchdog a boot-recovery ELŐTT fegyverződik – eddig egy elutasított boot-akció leszerelte a watchdogot, vagyis a kapu pont abban az esetben bukott volna, amiért létezik; a lease felülről korlátos (`MAX_TTL_MS` 1 óra, elutasítva nem levágva); a `service-start` nem parancs, hanem a stop undója a meglévő `clear`-en át, így az orchestrator recovery-útja osztályfüggetlen; a fordítás apply- és recovery-irányban EGY közös switch, ezért „applyban támogatott, recoveryban kihagyott” – vagyis hamis `allClear` egy még leállt masternode felett – nem leírható; minden lab-próba bezárva (egy leállt konténerbe `docker exec` eddig kidobott és a runt findings nélkül `recovery`-ben ragasztotta), és a próbák sorrendben futnak (`serviceRunning` először, mert az `docker inspect` és leállt konténerre is felel)
Módosított fájlok: `simulator/netemLease.ts` (FaultClass, FaultJob, undoFor, planServiceStop, planSweep, planBootRecovery, parseWrapperState); `simulator/netemRunner.ts` (LabFaultRunner, stopService, best-effort sweep, boot-retain, MAX_TTL_MS, service-stop parancs, exhaustive dispatch); `simulator/netemWrapperHost.ts` (dockerServiceArgv, dockerFaultExecutor, op-szerinti benign-predikátum, védett state-olvasás, drain parse-before-rm karanténnal); `simulator/netemWrapperMain.ts` (felfegyverzési sorrend); `simulator/liveExecutorPlan.ts` (labFaultsForPlan közös switch, service-stop, UnscheduledLiveFaultError, assertSingleFaultClass); `simulator/dockerLiveExecutor.ts` (safeProbe, osztályonkénti várakozás, faultStateClear osztályonkénti jelentése, allClear nem lehet vákuum); `simulator/labCompose.ts` (`container_name`, `restart: no`, `stop_grace_period`); `routes/v1/simulationAdmin.v1.routes.ts` (422/403); `simulator/labAcceptance.ts` (5. szekció); minden érintett tesztfájl
Futtatott tesztek és eredményük: `npm run typecheck && npm test && npm run build` zöld; 52 tesztfájl, 409 teszt (+36). Az elfogadási teszt (`node server/dist/simulator/labAcceptance.js`, kézi, nem `npm test` része) valós Dockeren ÁTMENT mind az 5 szekcióban – köztük: leállított konténer visszatér a wrapper saját TTL-jén orchestrator nélkül; halott wrapper után friss folyamat a rekordból állítja vissza; egy elérhetetlen konténer sikertelen visszavonása megmarad `expiresAtMs: 0`-val és nem blokkolja a többi visszaállítását
Nyitott hibák: ismert kódhiba nincs. AMI ÉLESEN FUT és ami NEM: `mn-stop`, `host-outage`, `quorum-member-outage`, `staker-stop` élesen fut; `network-degradation` élesen fut; `restart-flapping` (>=2 ciklus) és minden `notBeforeOffsetMs > 0` stop ELUTASÍTVA (`UnscheduledLiveFaultError`), mert nincs diszpécser – egyetlen `SimulationAction` sor sem íródik tervből és a `claimDue`-nak nincs hívója, így a ciklusok egyetlen stoppá esnének össze, miközben minden action „alkalmazva” lenne jelentve; `partition-apply` (node-isolation) ELUTASÍTVA, a mechanizmus (iptables/nft, saját watchdoggal) a 9. napé. TUDATOS KORLÁT: a párosított `service-start` nincs diszpécselve – egy felügyelet nélküli node az operátor `/recover`-jére vagy `durationSeconds + 120s`-nél (a tervező saját grace-e) jön vissza, tehát a tényleges kiesési ablak a névleges fölé lóghat; a run rekordjában ezt ki kell mondani, nem következtetni. KORLÁT (Item 5, változatlan): a recovery findings a run-dokumentumban élnek, nem az audit-streamben. ÜZEMELTETÉS: a wrapper csak ELŐRE gördíthető, amíg service-job él (egy régi bináris a service-jobot netemként olvasná és `tc qdisc del`-t próbálna egy leállt konténeren); a `state.json` törlése a labor lebontásának része
Következő pontos feladat: 9. nap – Docker/regtest scenario-k a Gate A-hoz. Előbb tisztázandó a felhasználóval: a „5/10 node restart storm” N node egyszeri újraindítása (ez MA fut) vagy N ciklus node-onként (ez diszpécsert igényel, és akkor a diszpécser 8.5. napi blokkoló). Utána a partition-mechanizmus (iptables/nft, lease + watchdog ugyanazzal a mintával, a `FaultClass` harmadik tagjaként), majd a scenario-futtatások élő action-loggal, abort közben, ismételhetőségi teszttel azonos seed mellett
Külső állapot/VPS-művelet történt-e: nem – minden lokális Docker (`defcon-core:test`), a `lab-accept` konténer a teszt végén törölve
Aktív fault vagy recovery timer: nincs
Felhasználói jóváhagyás szükséges-e: a 9. napi lokális Docker-munkához nem; bármilyen VPS-művelethez és a 13. napi pilothoz igen
