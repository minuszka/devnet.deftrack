Aktuális nap/fázis: 8. nap – block 1 (executor-váz, lease, recovery) kész és tesztelt; a nap NINCS lezárva – block 2 (Docker executor + peerező compose-generátor + netem/lease wrapper + elfogadási teszt) hátravan, a docker daemonra vár
Branch és HEAD: `feat/simulator-day8-block1` – block-1 commitok `0c1b90f..7e16d1f` plusz ez a handoff-frissítés
Elkészült (block 1): staking host-attribúció immutable, ablakra szűrt `StakeScriptObservation` rekordokból, nem a mutálódó `HostStatus`-ból (a #14 PeerObservation-mintája az attribúcióra) – a report ujjlenyomata így nem csúszik el, `verify()`/`REPORT_CONFLICT` megszűnik; finalize-előfeltétel, ami fail-closed elutasít, amíg egy mért kör `pending` vagy még a poller re-read sávjában van (a verdiktet érő `QuorumRound`/`Block` reprodukálhatóság); hálózati őr, ami live futást csak a lab-hálózaton (regtest) enged az executor-határon; a preflight maszkolt (`|| 'not-installed'`) wrapper-version félrekonfigurálásának feloldása, tiszta hibaüzenettel; `SimulationActionRepository` (atomikus `findOneAndUpdate` claim, lease-megújítás, `attempts`, expire – tiszta lease-builderekkel); reconcile sweep, ami az árva futást magától recovery-be viszi, `STALE_EVENT`-kihagyással; a `SimulationRun.recovery` CAS-a és audit-képe
Módosított fájlok: `StakeScriptObservation` modell + `domain/stakeAttribution`; measurement repository/service és `simulator/measurementWindows` (finalize gate) + `simulationMeasurement` evidence.tipHeight; `simulationControl.service` (network guard); `simulator/preflight` + `simulationEvidence.service` + `.env.example`; `services/simulationAction.repository` + `domain/actionLease`; `domain/reconcileSweep` + `services/simulationReconcile.service` + `index.ts` wiring; `simulationPersistence.service` + `simulationMongo.repository` (recovery CAS); a `seedStatus.service` és `peers.v1.routes` stake-script write-path; minden érintett tesztfájl
Futtatott tesztek és eredményük: `npm run typecheck && npm test && npm run build` zöld; 44 tesztfájl, 310 teszt; shared/server/client typecheck és production build sikeres
Nyitott hibák: ismert kódhiba nincs. KORLÁT (Item 5, recovery audit-kép): a recovery findings a run-dokumentumban élnek, nem az audit-streamben – a `loadRun` hiányzó-projekció ága `insertRun`-t hív, ami `$setOnInsert`-tel ÜRES `recovery`-t ír, így ha a run-doc valaha elvész vagy törlődik, az állapot a streamből újraépül, a recovery findings viszont VÉGLEGESEN elvesznek, és semmi nem jelzi, hogy voltak. A teljes audit-rekonstrukció dedikált audit-mezőt (`recoveryAfter`) + replay-változtatást igényelne – önálló, óvatos lépés, nem blokkoló. Az automatikus report-finalize a finalize-előfeltétellel már biztonságosan bekötheto˝, de a hiteles fault start/end block anchor még a block-2 executorból érkezik
Következő pontos feladat: 8. nap block 2 – daemon-független rész előbb (peerező compose-generátor + a kimeneti YAML unit-tesztje: `-addnode`/`-connect` jelen, `-discover=0`/`-dnsseed=0` nem hagyja vakon a node-okat; Dockerfile a `COPY`-zott `defcond` köré, konténerben `ldd <bin> | grep "not found"`; netem/lease wrapper állapotgépe tiszta függvényként teszttel: job-id, TTL, idempotens apply/clear, boot-kori takarítás, a `tc`/iptables hívás injektált végrehajtóval), majd a docker daemon (Docker Desktop WSL-integráció) fenn: integrációs futtatás és az elfogadási teszt – fault közben leállított orchestrator után a hálózat magától visszaáll
Külső állapot/VPS-művelet történt-e: nem
Aktív fault vagy recovery timer: nincs
Felhasználói jóváhagyás szükséges-e: a block-2 lokális Docker-munkához nem; bármilyen VPS-művelethez és a 13. napi pilothoz igen
