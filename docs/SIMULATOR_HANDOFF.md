Aktuális nap/fázis: 8. nap LEZÁRVA, és lezárva a rá következő külső audit teljes javítási listája is (1-4. tétel). A 9. nap (Docker/regtest scenario-k, Gate A) NEM kezdődött el, és a labor még nem tud élő futamot élesíteni -- lásd a Nyitott hibáknál, ez a következő valódi akadály
Branch és HEAD: `fix/live-idempotency` (a lista utolsó darabja + ez a frissítés); minden korábbi a mainben, PR #35, #37, #41, #42, #44, #45
Elkészült a 8. napból: node-local netem wrapper daemon egyetlen tulajdonossal és saját TTL-watchdoggal; második fault-osztály (konténer stop/start), (konténer, osztály) párra kulcsolt állapottal és a jobból származtatott undóval; valós `DockerLiveExecutor` a control-API három slotjában, fail-closed minden fault-fajtára, amit nem tud hitelesen alkalmazni; kompozit netem (latency+jitter+loss egy qdisc-ben); coincidence-preset az `IsMiningPhase` pontos reprodukciójaként, mind a hat profilra
Elkészült az auditból: (1) az observer containerenként a SAJÁT node-ját kérdezi, és amit nem ér el, arról NEM jelent -- a compose ehhez node-onként publikál RPC-portot, mert az image-ben nincs `defcon-cli`; (2) a regiszter kétlépcsős modellje kikényszerítve (a deklaráció nem tud engedélyezni; az engedélyezés safety-admin), plusz Compose-projekt allowlist az executoron, az első enqueue ELŐTT; (3) a cooldown határideje az állapotra kerül belépéskor (opcionális mező, `canonicalJson` ejti az `undefined`-ot, ezért a korábbi ujjlenyomatok bitre azonosak); (4a) a parancs claim-elt, nem fogyasztott: pending -> inflight -> gone, at-least-once, mert minden wrapper-parancs idempotens, és a kísérlet a CLAIM pillanatában íródik ki, hogy egy folyamatot megölő parancs is karanténba kerüljön; (4b) a lease abszolút pillanat, a már lejártat elutasítja (a `minLeaseMs` padló megszűnt); (4c) a live lock atomi CAS-szal, a futam saját borítékát használva lease-nek, mindkét recovery-ágon elengedve; (4d) egy megismételt start/recover a rekordból válaszol, mert minden esemény-azonosító a request-kulcsból származik
Elkészült a laborhoz: külön `labServer.ts` entrypoint saját, KÖTELEZŐEN külön adatbázissal (nincs alapérték; elutasít, ha egyenlő a `MONGODB_URI`-val vagy ugyanazt az adatbázist nevezi bármely hoston); célpont-regiszter írója az admin API-n (deklarálva, sosem következtetve); wrapper-heartbeat, amiből az élő preflight a recovery-bizonyítékot olvassa; blokk-indexer a laborban (az `explorer-synced` required check enélkül átjárhatatlan kaput hagyott); `ops/lab-observer.mjs`; a control-API hibái szerveroldali naplót kapnak, mert egy 500 korábban NYOMTALAN volt
Módosított fájlok: `simulator/netemLease|netemRunner|netemWrapperHost|netemWrapperMain|labCompose|liveExecutorPlan|dockerLiveExecutor|wrapperHeartbeat|targetRegistration|labAcceptance|dkgCoincidence`; `domain/simulationRunState|labIsolation|liveRunLock`; `services/simulationControl|simulationEvidence|simulationLiveRunLock`; `routes/v1/simulationAdmin.v1`; `labServer.ts`; `config.ts`; `ops/lab-observer.mjs`; `.env.example`; minden érintett tesztfájl
Futtatott tesztek és eredményük: `npm run typecheck && npm test && npm run build` mind 0-val tér vissza; 57 tesztfájl, 489 teszt. FIGYELEM: a kapukat exit-kóddal kell ellenőrizni -- a `| tail -1` elnyeli, és egyszer emiatt commitoltam bukó typecheck mellett. Az elfogadási teszt (`node server/dist/simulator/labAcceptance.js`, kézi) valós Dockeren mind az 5 szekcióban ÁTMENT a tartós queue-val, az abszolút lease-szel és a projekt-allowlisttel
Nyitott hibák: ismert kódhiba nincs. A LABOR NEM TUD ÉLŐ FUTAMOT ÉLESÍTENI, és ez a következő valódi akadály: a `labServer` a sync-et indítja, de a masternode-, quorum- és ChainLock-kollektort nem, a compose-node-ok pedig sima regtest daemonok, nem regisztrált masternode-ok -- így masternode-célpont nem oldható fel (`MISSING_PROTX_MAPPING`), quorum-scenario nem talál tagot, és az `arm()` baseline-ja nem éri el a három DKG-kört. Végigmérve idáig jut: create + validate egy `staker` célponttal ÁTMEGY, live start nincs bizonyítva. Tudatosan halasztva: a `hostRef` szétválasztása (`physicalHostId` / `executionContainerRef`) -- enélkül a "10 MN egy hoston" nem képezhető le; és a heartbeat boot-kori qdisc-próbája -- ma az állapotfájlból következtet tisztaságra, így egy összeomlott elődtől bent maradt netem mellett is átengedne. Roadmap szerint hiányzik: action-diszpécser, automatikus `begin_observation`, automatikus measurement finalize, restart-flapping, staggered reconnect, partition
Külső állapot: VPS-műveletet nem végeztünk. Lokálisan eldobható lab-konténerek futhatnak (`lab-mongo` 27018, `lab-node` 19998, `mn01`/`mn02` a generált compose-ból); leszedés `docker rm -f`. A megosztott WSL MongoDB-hez NEM nyúltunk: nincs benne admin-felhasználó, amivel új adatbázisra jogot lehetne adni, ezért kapott a labor saját példányt. A `~/DEFCON` checkoutban egy másik session dolgozik
Aktív fault vagy recovery timer: nincs
Felhasználói jóváhagyás szükséges-e: a lokális Docker-munkához nem; bármilyen VPS-műveletnek, a 13. napi pilotnak és minden devnet-fault futtatásnak igen
Egyéb, amit tudni kell: a lab image-ben NINCS `defcon-cli`, csak `defcond` -- a node-ot csak HTTP RPC-n lehet kérdezni. A célpont `expectedBuild`-je 64 hex, tehát sha256, nem a flottánál szokásos md5. Egy node-összeomlást találtunk a lab regtesten (`masternode/payments.cpp:55`, null egy `gsl::not_null`-ban, `std::terminate`, RPC-parancs közben, a mi indexerünk `masternode payments` hívása váltotta ki); determinisztikus kiváltót NEM izoláltunk, és a `std::terminate` nem flush-ol, ezért a datadir 250 blokkról 1-re jött vissza. Ez Core-oldali, nem az explorer hibája
