# Normalise to LF in the repository. Shell scripts and .env files break on a
# Linux host when checked out with CRLF (`$'\r': command not found`).
* text=auto eol=lf
*.sh    text eol=lf
*.env   text eol=lf
.env.example text eol=lf
*.png   binary
*.jpg   binary
*.ico   binary
